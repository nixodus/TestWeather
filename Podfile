# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestWeather3' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestWeather3

  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'SVProgressHUD'

  target 'TestWeather3Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestWeather3UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
